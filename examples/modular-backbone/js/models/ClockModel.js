define([
	'underscore',
	'backbone',
	'Utils',
	], function(_, Backbone, Utils) {

		var ClockModel = Backbone.Model.extend({

			initialize: function(){
								
				
			}





		});

		return ClockModel;

	});
